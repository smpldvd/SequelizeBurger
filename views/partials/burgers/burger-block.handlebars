<tr class="table-row">
    <td class="table-first-col">{{id}}. {{burger_name}}</td>
    <td>
        <button class="change-devour" data-id="{{id}}">Eat up!</button>
    </td>
</tr>

{{!-- <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
    <input type="hidden" type="text" name="burger_id" value={{this.id}}>
    <br> {{!-- enter eater name here --}}
    <input class="my-1" type="text" name="eaten_by" placeholder="Eaten by">
    <br>
    <button type="submit" class="btn btn-default">Devour it!</button>
</form> --}}